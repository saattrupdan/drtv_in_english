{
  "name": "drtv-in-english-extension",
  "private": true,
  "version": "0.1.0-dev",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --package",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "sharp": "^0.34.5",
    "typescript": "^5.5.0"
  }
}
